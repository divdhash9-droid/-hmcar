# قائمة التحقق لنشر مشروع Node.js/Express

## 1. متغيرات البيئة (Environment Variables)
- ضبط جميع المتغيرات في ملف `.env` أو `vercel.env` (مثال: MONGO_URI، SECRET_KEY، إلخ)
- عدم رفع ملفات البيئة الحساسة إلى Git (تأكد من وجودها في `.gitignore`)

## 2. تثبيت الحزم
- تنفيذ الأمر: `npm install`
- التأكد من تحديث ملف `package.json`

## 3. اختبار الوظائف الأساسية
- تشغيل جميع اختبارات smoke/unit
- التأكد من عمل جميع المسارات (routes) بدون أخطاء

## 4. إعدادات التكوين
- مراجعة ملفات config (مثل config/database.js)
- التأكد من إعدادات البيئة (production/development)

## 5. الأمان
- تفعيل middlewares مثل Helmet, CORS, rate limiting
- التأكد من عدم وجود بيانات حساسة في الكود

## 6. ملفات النشر
- إعداد ملفات Docker أو docker-compose إذا لزم الأمر
- التأكد من إعداد ملفات vercel.json أو ما يعادلها

## 7. التوثيق
- مراجعة ملفات الدليل (README, DEPLOYMENT_GUIDE.md)
- تحديث أي تعليمات ناقصة أو غير واضحة

## 8. التحقق النهائي
- تشغيل التطبيق محلياً على المنفذ الصحيح (عادة 4000)
- التأكد من ظهور جميع الصفحات والوظائف بشكل سليم
- مراجعة سجل الأخطاء (logs) والتأكد من عدم وجود مشاكل

---

**ملاحظة:**
اتبع هذه القائمة خطوة بخطوة قبل كل عملية نشر لضمان عمل المشروع بدون مشاكل.