<!-- [[ARABIC_HEADER]] هذا الملف (views/client/notification-settings.ejs) جزء من مشروع HM CAR ويحتوي تعليقات عربية لضمان الوضوح. -->

<% layout('layout') -%>

<div class="container mt-5">
    <h2>إعدادات الإشعارات</h2>
    <p class="text-muted">تحكم في أنواع الإشعارات التي تصلك ووسيلة الإرسال.</p>
    <hr>

    <form id="preferences-form">
        <div id="alert-container"></div>

        <h4>إشعارات المزادات</h4>
        <p class="text-muted">اختر الأحداث المتعلقة بالمزادات التي ترغب باستلام إشعار بها.</p>
        <div class="row">
            <div class="col-md-6">
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" id="outbid" name="outbid">
                    <label class="form-check-label" for="outbid">عند المزايدة ضدي</label>
                </div>
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" id="auctionEndingSoon" name="auctionEndingSoon">
                    <label class="form-check-label" for="auctionEndingSoon">عند اقتراب نهاية مزاد أتابعه</label>
                </div>
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" id="newBidOnWatched" name="newBidOnWatched">
                    <label class="form-check-label" for="newBidOnWatched">مزايدات جديدة على العناصر التي أتابعها</label>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" id="auctionWon" name="auctionWon">
                    <label class="form-check-label" for="auctionWon">عند الفوز بمزاد</label>
                </div>
                <div class="form-check form-switch mb-3">
                    <input class="form-check-input" type="checkbox" id="auctionLost" name="auctionLost">
                    <label class="form-check-label" for="auctionLost">عند خسارة مزاد شاركت به</label>
                </div>
            </div>
        </div>

        <hr class="my-4">

        <h4>التواصل</h4>
        <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="newMessage" name="newMessage">
            <label class="form-check-label" for="newMessage">رسائل جديدة</label>
        </div>

        <hr class="my-4">

        <h4>تحديثات المنصة</h4>
        <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="systemUpdates" name="systemUpdates">
            <label class="form-check-label" for="systemUpdates">تحديثات النظام والتنبيهات الأمنية</label>
        </div>
        <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="promotions" name="promotions">
            <label class="form-check-label" for="promotions">العروض والميزات الجديدة</label>
        </div>

        <hr class="my-4">

        <h4>قنوات الإرسال</h4>
        <p class="text-muted">اختر وسيلة استلام الإشعارات. قد تُرسل بعض الإشعارات المهمة عبر البريد مهما كانت الإعدادات.</p>
        
        <div class="row">
            <div class="col-md-6">
                <h5><i class="fas fa-envelope me-2"></i>البريد الإلكتروني</h5>
                <div class="form-check">
                    <input class="form-check-input email-channel" type="checkbox" id="email-outbid" data-key="outbid">
                    <label class="form-check-label" for="email-outbid">عند المزايدة ضدي</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input email-channel" type="checkbox" id="email-auctionWon" data-key="auctionWon">
                    <label class="form-check-label" for="email-auctionWon">عند الفوز بمزاد</label>
                </div>
                 <div class="form-check">
                    <input class="form-check-input email-channel" type="checkbox" id="email-promotions" data-key="promotions">
                    <label class="form-check-label" for="email-promotions">العروض</label>
                </div>
            </div>
            <div class="col-md-6">
                <h5><i class="fas fa-bell me-2"></i>إشعارات المتصفح</h5>
                <div class="form-check">
                    <input class="form-check-input push-channel" type="checkbox" id="push-outbid" data-key="outbid">
                    <label class="form-check-label" for="push-outbid">عند المزايدة ضدي</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input push-channel" type="checkbox" id="push-newMessage" data-key="newMessage">
                    <label class="form-check-label" for="push-newMessage">رسائل جديدة</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input push-channel" type="checkbox" id="push-auctionEndingSoon" data-key="auctionEndingSoon">
                    <label class="form-check-label" for="push-auctionEndingSoon">اقتراب نهاية المزاد</label>
                </div>
            </div>
        </div>

        <div class="mt-5">
            <button type="submit" class="btn btn-primary btn-gold">حفظ الإعدادات</button>
        </div>
    </form>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('preferences-form');
    const alertContainer = document.getElementById('alert-container');

    // Fetch and populate existing preferences
    fetch('/notifications/preferences', { headers: { 'Accept': 'application/json' } })
        .then(res => res.json())
        .then(data => {
            if (data) {
                // Main toggles
                Object.keys(data).forEach(key => {
                    const input = document.getElementById(key);
                    if (input && typeof data[key] === 'boolean') {
                        input.checked = data[key];
                    }
                });
                // Email channel toggles
                if(data.emailNotifications){
                    document.querySelectorAll('.email-channel').forEach(input => {
                        const key = input.dataset.key;
                        if(data.emailNotifications[key] !== undefined){
                            input.checked = data.emailNotifications[key];
                        }
                    });
                }
                // Push channel toggles
                 if(data.pushNotifications){
                    document.querySelectorAll('.push-channel').forEach(input => {
                        const key = input.dataset.key;
                        if(data.pushNotifications[key] !== undefined){
                            input.checked = data.pushNotifications[key];
                        }
                    });
                }
            }
        })
        .catch(err => console.error('Failed to load preferences:', err));

    // Handle form submission
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        const formData = new FormData(form);
        const data = {};
        const emailNotifications = {};
        const pushNotifications = {};

        // Main toggles
        for (let [key, value] of formData.entries()) {
            const input = document.getElementById(key);
            if (input.type === 'checkbox') {
                 data[key] = input.checked;
            }
        }
        
        // Channel toggles
        document.querySelectorAll('.email-channel').forEach(input => {
            emailNotifications[input.dataset.key] = input.checked;
        });
        document.querySelectorAll('.push-channel').forEach(input => {
            pushNotifications[input.dataset.key] = input.checked;
        });

        data.emailNotifications = emailNotifications;
        data.pushNotifications = pushNotifications;

        fetch('/notifications/preferences', {
            method: 'PATCH',
            headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
            },
            body: JSON.stringify(data)
        })
        .then(res => res.json())
        .then(result => {
            if (result && !result.error) {
                showAlert('تم حفظ الإعدادات بنجاح', 'success');
            } else {
                showAlert(result && result.error ? result.error : 'تعذر حفظ الإعدادات', 'danger');
            }
        })
        .catch(err => {
            console.error('Error saving preferences:', err);
            showAlert('حدث خطأ. حاول مرة أخرى.', 'danger');
        });
    });

    function showAlert(message, type) {
        alertContainer.innerHTML = `<div class="alert alert-${type} alert-dismissible fade show" role="alert">
            ${message}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>`;
    }
});
</script>
