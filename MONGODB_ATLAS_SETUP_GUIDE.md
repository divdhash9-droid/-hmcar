# دليل إعداد MongoDB Atlas

## 🎯 المشكلة الحالية

عنوان IP الحالي: `176.123.18.39/32` غير مدرج في القائمة البيضاء لـ MongoDB Atlas.

---

## 🔧 خطوات الإصلاح

### 1. تسجيل الدخول إلى MongoDB Atlas
1. اذهب إلى [MongoDB Atlas](https://cloud.mongodb.com/)
2. سجل الدخول بحسابك
3. اختر cluster الخاص بك

### 2. إضافة IP إلى القائمة البيضاء
1. من لوحة التحكم، اذهب إلى **Network Access**
2. انقر على **Add IP Address**
3. اختر **Add Current IP Address** أو أضف يدوياً:
   ```
   176.123.18.39/32
   ```
4. انقر على **Confirm**

### 3. التحقق من الإعدادات
تأكد من:
- ✅ IP مضاف إلى القائمة البيضاء
- ✅ مستخدم قاعدة البيانات لديه صلاحيات
- ✅ connection string صحيح

---

## 🔗 Connection String الحالي

من ملف `.env`:
```
MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASSWORD>@<CLUSTER_HOST>/car-auction?retryWrites=true&w=majority
```

---

## 🧪 اختبار الاتصال

بعد إضافة IP، قم بتشغيل:
```bash
node test-atlas-connection.js
```

أو:
```bash
node scripts/checkEnvironment.js
```

---

## 📝 ملاحظات هامة

### الأمان
- استخدام IP محدد أكثر أماناً من 0.0.0.0/0
- يمكن إضافة عدة عناوين IP إذا لزم الأمر

### البدائل
إذا لم تتمكن من إضافة IP:
1. استخدم VPN بعنوان IP مصرح به
2. أضف 0.0.0.0/0 (للتطوير فقط)
3. استخدم MongoDB محلي

---

## 🚀 الخطوات التالية

بعد إصلاح الاتصال:
1. أعد تشغيل فحص البيئة
2. تحقق من اتصال Redis
3. ابدأ تشغيل التطبيق

---

## 📞 المساعدة

إذا واجهت مشاكل:
- تحقق من صحة بيانات الاعتماد
- تأكد من أن Cluster يعمل
- تحقق من إعدادات الشبكة

---

*تم إعداد هذا الدليل لمساعدتك في إصلاح اتصال قاعدة البيانات*
